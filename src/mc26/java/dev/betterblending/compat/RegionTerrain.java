package dev.betterblending.compat;

/** The build output each section of a Sodium render region is currently drawn from. */
public interface RegionTerrain {
    Object betterBlending$mesh(int section);
}
